// Deklarace funkce
// Cyklus for let i = (parametr); i <= (parametr)
// console.log(i);

// Volání funkce

function printRange(start, end) {
    for (let i = start; i <= end; i++){
    console.log(i);
    }
}
printRange(1,4)